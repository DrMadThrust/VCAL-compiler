1, 0, 0
Const read
Const poll

f (x1, x2)
	x3 = +(x1, x2)
	temp = and(x2, x3) if x2 > 0
return (x1, temp)

g (x, y)
	var = read
	x, var = f(x, var)
	write(var)
	z = +(x, var)
	z = shift left(z)
	repeat (x, z) if z > 0
return (x)

main ()
	in = read
	test = 4
	g(in, test)
end
